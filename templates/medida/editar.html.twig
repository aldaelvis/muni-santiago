{% extends 'content.html.twig' %}

{% block contenido %}
    <div class="page-title-box">
        <div class="row align-items-center">
            <div class="col-sm-6">
                <h4 class="page-title">Medidas</h4>
            </div>

            <div class="col-sm-6">

                <div class="float-right d-none d-md-block">
                    <a href="{{ path('app_medida') }}" class="btn btn-secondary"><i class="mdi mdi-arrow-left"></i>Regresar</a>
                </div>

            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <form action="" method="post">

                        <div class="form-group ">
                            <label for="nombre">Nombre</label>
                            <input type="text" value="{{ medida.nombre }}" class="form-control" name="nombre"
                                   id="nombre">
                        </div>


                        <div class="form-group">
                            <label for="abreviacion">Abreviación</label>
                            <input type="text" value="{{ medida.abreviacion }}" class="form-control" name="abreviacion"
                                   id="abreviacion">
                        </div>


                        <div class="form-group mb-0">
                            <button type="submit" class="btn btn-primary waves-effect waves-light mr-1">
                                Editar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
