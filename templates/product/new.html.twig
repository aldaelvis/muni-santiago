{% extends 'content.html.twig' %}
{% block contenido %}

    <div class="page-title-box">
        <div class="row align-items-center">
            <div class="col-sm-6">
                <h4 class="page-title">Articulo</h4>
            </div>

            <div class="col-sm-6">

                <div class="float-right d-none d-md-block">
                    <a href="{{ path('product_index') }}" class="btn btn-secondary">
                        <i class="mdi mdi-arrow-left"></i>Regresar</a>
                </div>

            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                     {{ include('product/_form.html.twig', {button_text: 'Guardar'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}